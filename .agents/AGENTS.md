# Global Project Rules

## Backend Code Modification Constraint
- **Rule:** Do NOT modify, delete, or update any code in the `backend` directory.
- **Action:** The `backend` directory is STRICTLY read-only for agents. Always treat it as reference material.
- **Reason:** Requested by user to ensure the backend team's code remains untouched by automated AI actions.
